# TAISUN v2 - Ops Stack Environment Variables
# Copy to .env.ops and configure

# === Apprise (P16) ===
APPRISE_PORT=8000
APPRISE_DEBUG=0
# Comma-separated notification URLs
# Format: slack://token-a/token-b/token-c, discord://webhook-id/webhook-token
APPRISE_URLS=

# === GitHub (Required for Issue posting) ===
GITHUB_TOKEN=

# === Ops Schedule Runner (P18) ===
# Global enable/disable (default: false for safety)
OPS_SCHEDULE_ENABLED=false

# Timezone for schedule execution
OPS_SCHEDULE_TIMEZONE=Asia/Tokyo

# === Specific Jobs (configure in config/proxy-mcp/ops-schedule.json) ===
# Jobs are disabled by default. Enable in config file:
# - daily_observability_report
# - weekly_observability_report
# - weekly_improvement_digest

# === Docker Compose Profiles ===
# To enable ops-scheduler:
#   docker compose -f docker-compose.ops.yml --profile ops-scheduler up -d
